import React from 'react'

import './App.css'

const App = () => {
  const message = "Será que dá pra se sustentar com wordpress?"

  return <div className="container">{message}</div>
}

export default App